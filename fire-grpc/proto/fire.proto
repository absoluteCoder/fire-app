syntax = "proto3";

package fire;

option go_package = "gen/pb";

service FIREService {
  rpc Ignite (FireRequest) returns (FireResponse);
}

message FireRequest {
  string fuel = 1;
}

message FireResponse {
  string message = 1;
}